# 复制本文件为 .env 并修改(不要提交 .env 到仓库)
# 在仓库根目录(与 docker-compose.yml 同目录)执行 docker compose 时会自动读取

# 数据库 root 密码(db 与 backend 共用)
DB_PASSWORD=bookadmin123

# JWT 签名密钥：请改成一段足够长且随机的字符串(生产必改)
JWT_SECRET=change-this-to-a-long-random-string
